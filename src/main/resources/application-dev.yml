spring:
  jpa:
    show-sql: true

  liquibase:
    enabled: true

logging:
  level:
    com.portfolio.siniestros: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
